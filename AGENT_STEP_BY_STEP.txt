================================================================================
           FRONTEND REFACTORING - AGENT EXECUTION FORMAT
                    Copy One Step At A Time To Agent
================================================================================

INSTRUCTIONS FOR AGENT:
- Execute each command exactly as written
- Run in: /home/user/khmer_project/menu-scanner-frontend-client
- After completion, copy next step and send to agent
- Verify each step succeeds before moving to next

================================================================================
                              STEP 1 TO AGENT
================================================================================

TASK: Setup & Create Directory Structure (30 minutes)

Run these commands in order:

cd /home/user/khmer_project/menu-scanner-frontend-client
git checkout -b refactor/frontend-cleanup-phase-1
npm install
npm run build
npx tsc --noEmit

mkdir -p src/constants/api-endpoints
mkdir -p src/constants/ui-strings
mkdir -p src/constants/ui-timings
mkdir -p src/constants/app-routes
mkdir -p src/docs
mkdir -p src/scripts
mkdir -p src/hooks
mkdir -p src/services/api
mkdir -p src/services/utilities

npm run build && npx tsc --noEmit

VERIFY: No errors, build succeeds, TypeScript compiles
If successful, move to STEP 2

================================================================================
                              STEP 2 TO AGENT
================================================================================

TASK: Create API Endpoints Constants (30 minutes)

Create file: src/constants/api-endpoints/index.ts

cat > src/constants/api-endpoints/index.ts << 'EOF'
export const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3001/api';

export const AUTH_ENDPOINTS = {
  LOGIN: '/auth/login',
  REGISTER: '/auth/register',
  LOGOUT: '/auth/logout',
  REFRESH_TOKEN: '/auth/refresh-token',
  GET_PROFILE: '/auth/profile',
  UPDATE_PROFILE: '/auth/profile/update',
} as const;

export const PRODUCT_ENDPOINTS = {
  GET_ALL: '/products',
  GET_ONE: (id: string) => `/products/${id}`,
  CREATE: '/products',
  UPDATE: (id: string) => `/products/${id}`,
  DELETE: (id: string) => `/products/${id}`,
  SEARCH: '/products/search',
  GET_BY_CATEGORY: (categoryId: string) => `/products/category/${categoryId}`,
  GET_BY_BRAND: (brandId: string) => `/products/brand/${brandId}`,
} as const;

export const CART_ENDPOINTS = {
  GET: '/cart',
  ADD_ITEM: '/cart/items',
  REMOVE_ITEM: (itemId: string) => `/cart/items/${itemId}`,
  UPDATE_ITEM: (itemId: string) => `/cart/items/${itemId}`,
  CLEAR: '/cart/clear',
  CHECKOUT: '/cart/checkout',
} as const;

export const CATEGORY_ENDPOINTS = {
  GET_ALL: '/categories',
  GET_ONE: (id: string) => `/categories/${id}`,
  CREATE: '/categories',
  UPDATE: (id: string) => `/categories/${id}`,
  DELETE: (id: string) => `/categories/${id}`,
} as const;

export const BRAND_ENDPOINTS = {
  GET_ALL: '/brands',
  GET_ONE: (id: string) => `/brands/${id}`,
  CREATE: '/brands',
  UPDATE: (id: string) => `/brands/${id}`,
  DELETE: (id: string) => `/brands/${id}`,
} as const;

export const BUSINESS_ENDPOINTS = {
  GET_PROFILE: '/business/profile',
  UPDATE_PROFILE: '/business/profile',
  GET_SETTINGS: '/business/settings',
  UPDATE_SETTINGS: '/business/settings',
  GET_LOCATIONS: '/business/locations',
  CREATE_LOCATION: '/business/locations',
  UPDATE_LOCATION: (id: string) => `/business/locations/${id}`,
  DELETE_LOCATION: (id: string) => `/business/locations/${id}`,
} as const;

export const ORDER_ENDPOINTS = {
  GET_ALL: '/orders',
  GET_ONE: (id: string) => `/orders/${id}`,
  CREATE: '/orders',
  UPDATE_STATUS: (id: string) => `/orders/${id}/status`,
  CANCEL: (id: string) => `/orders/${id}/cancel`,
  GET_HISTORY: '/orders/history',
} as const;

export const PROMOTION_ENDPOINTS = {
  GET_ALL: '/promotions',
  GET_ONE: (id: string) => `/promotions/${id}`,
  CREATE: '/promotions',
  UPDATE: (id: string) => `/promotions/${id}`,
  DELETE: (id: string) => `/promotions/${id}`,
} as const;

export const USER_ENDPOINTS = {
  GET_ALL: '/users',
  GET_ONE: (id: string) => `/users/${id}`,
  CREATE: '/users',
  UPDATE: (id: string) => `/users/${id}`,
  DELETE: (id: string) => `/users/${id}`,
} as const;

export const REVIEW_ENDPOINTS = {
  GET_ALL: '/reviews',
  GET_BY_PRODUCT: (productId: string) => `/reviews/product/${productId}`,
  CREATE: '/reviews',
  UPDATE: (id: string) => `/reviews/${id}`,
  DELETE: (id: string) => `/reviews/${id}`,
} as const;

export const ANALYTICS_ENDPOINTS = {
  GET_SALES: '/analytics/sales',
  GET_TOP_PRODUCTS: '/analytics/top-products',
  GET_USER_BEHAVIOR: '/analytics/user-behavior',
  GET_INVENTORY_REPORT: '/analytics/inventory',
} as const;
EOF

npm run build && npx tsc --noEmit

VERIFY: File created, build succeeds
If successful, move to STEP 3

================================================================================
                              STEP 3 TO AGENT
================================================================================

TASK: Create UI Strings Constants (30 minutes)

Create file: src/constants/ui-strings/index.ts

cat > src/constants/ui-strings/index.ts << 'EOF'
export const NAVIGATION_STRINGS = {
  HOME: 'Home',
  PRODUCTS: 'Products',
  PROMOTIONS: 'Promotions',
  CATEGORIES: 'Categories',
  BRANDS: 'Brands',
  CART: 'Cart',
  FAVORITES: 'Favorites',
  ACCOUNT: 'Account',
} as const;

export const ACTION_STRINGS = {
  SAVE: 'Save',
  CANCEL: 'Cancel',
  DELETE: 'Delete',
  EDIT: 'Edit',
  ADD: 'Add',
  REMOVE: 'Remove',
  SUBMIT: 'Submit',
  CONFIRM: 'Confirm',
  CLOSE: 'Close',
  BACK: 'Back',
  NEXT: 'Next',
  PREVIOUS: 'Previous',
  SEARCH: 'Search',
  FILTER: 'Filter',
  SORT: 'Sort',
} as const;

export const PRODUCT_STRINGS = {
  PRODUCT: 'Product',
  PRICE: 'Price',
  QUANTITY: 'Quantity',
  ADD_TO_CART: 'Add to Cart',
  IN_STOCK: 'In Stock',
  OUT_OF_STOCK: 'Out of Stock',
  SELECT_SIZE: 'Please select a size',
  SELECT_VARIANT: 'Please select a variant',
} as const;

export const CART_STRINGS = {
  CART: 'Shopping Cart',
  EMPTY_CART: 'Your cart is empty',
  SUBTOTAL: 'Subtotal',
  TOTAL: 'Total',
  ITEM_ADDED: 'Item added to cart',
  ITEM_REMOVED: 'Item removed from cart',
} as const;

export const AUTH_STRINGS = {
  LOGIN: 'Login',
  REGISTER: 'Register',
  LOGOUT: 'Logout',
  EMAIL: 'Email',
  PASSWORD: 'Password',
  REMEMBER_ME: 'Remember me',
  FORGOT_PASSWORD: 'Forgot password?',
  LOGIN_SUCCESS: 'Logged in successfully',
  LOGOUT_SUCCESS: 'Logged out successfully',
} as const;

export const ERROR_STRINGS = {
  SOMETHING_WENT_WRONG: 'Something went wrong',
  TRY_AGAIN: 'Please try again',
  LOADING_ERROR: 'Failed to load data',
  INVALID_CREDENTIALS: 'Invalid email or password',
} as const;

export const SUCCESS_STRINGS = {
  SUCCESS: 'Success',
  SAVED: 'Saved successfully',
  DELETED: 'Deleted successfully',
  CREATED: 'Created successfully',
} as const;
EOF

npm run build && npx tsc --noEmit

VERIFY: File created, build succeeds
If successful, move to STEP 4

================================================================================
                              STEP 4 TO AGENT
================================================================================

TASK: Create UI Timings Constants (20 minutes)

Create file: src/constants/ui-timings/index.ts

cat > src/constants/ui-timings/index.ts << 'EOF'
export const DEBOUNCE_TIMINGS = {
  SEARCH: 300,
  FILTER: 500,
  FORM_INPUT: 300,
  SCROLL: 150,
} as const;

export const ANIMATION_DURATIONS = {
  FAST: 150,
  BASE: 200,
  SLOW: 300,
} as const;

export const TIMEOUT_DURATIONS = {
  SHORT: 3000,
  BASE: 5000,
  LONG: 10000,
  TOAST: 3000,
} as const;

export const CACHE_DURATIONS = {
  SHORT: 60000,
  MEDIUM: 300000,
  LONG: 600000,
} as const;
EOF

npm run build && npx tsc --noEmit

VERIFY: File created, build succeeds
If successful, move to STEP 5

================================================================================
                              STEP 5 TO AGENT
================================================================================

TASK: Create Routes Constants (20 minutes)

Create file: src/constants/app-routes/navigation.ts

cat > src/constants/app-routes/navigation.ts << 'EOF'
export const PUBLIC_ROUTES = {
  HOME: '/',
  PRODUCTS: '/products',
  PROMOTIONS: '/promotions',
  CATEGORIES: '/categories',
  BRANDS: '/brands',
} as const;

export const AUTH_ROUTES = {
  LOGIN: '/auth/login',
  REGISTER: '/auth/register',
  FORGOT_PASSWORD: '/auth/forgot-password',
  RESET_PASSWORD: '/auth/reset-password',
} as const;

export const CUSTOMER_ROUTES = {
  DASHBOARD: '/customer/dashboard',
  PROFILE: '/customer/profile',
  ORDERS: '/customer/orders',
  FAVORITES: '/customer/favorites',
  SETTINGS: '/customer/settings',
} as const;

export const ADMIN_ROUTES = {
  DASHBOARD: '/admin',
  PRODUCTS: '/admin/products',
  ORDERS: '/admin/orders',
  USERS: '/admin/users',
  ANALYTICS: '/admin/analytics',
} as const;

export const NAVIGATION_LINKS = [
  { name: 'Home', href: PUBLIC_ROUTES.HOME },
  { name: 'Products', href: PUBLIC_ROUTES.PRODUCTS },
  { name: 'Promotions', href: PUBLIC_ROUTES.PROMOTIONS },
  { name: 'Categories', href: PUBLIC_ROUTES.CATEGORIES },
  { name: 'Brands', href: PUBLIC_ROUTES.BRANDS },
] as const;
EOF

npm run build && npx tsc --noEmit

VERIFY: File created, build succeeds
If successful, move to STEP 6

================================================================================
                              STEP 6 TO AGENT
================================================================================

TASK: Audit Components (1 hour)

Run these commands:

find src/components -name "*.tsx" -exec wc -l {} \; | sort -rn | head -15

EXPECTED OUTPUT: List of components with line counts

Then run:

grep -r "console\.log" src --include="*.tsx" --include="*.ts" | wc -l

EXPECTED OUTPUT: Number of console.log statements

Then run:

find src/components -type f -name "*.tsx" | wc -l

EXPECTED OUTPUT: Total number of components

VERIFY: Audit complete, metrics gathered
If successful, move to STEP 7

================================================================================
                              STEP 7 TO AGENT
================================================================================

TASK: Create Custom Hook - usePickerSize (45 minutes)

Create file: src/hooks/use-size-picker.ts

cat > src/hooks/use-size-picker.ts << 'EOF'
import { useState, useCallback, useMemo } from 'react';

export interface Size {
  id: string;
  name: string;
  available: boolean;
}

export function usePickerSize({
  initialSizeId = null,
  sizes,
}: {
  initialSizeId?: string | null;
  sizes: Size[];
}) {
  const [selectedSizeId, setSelectedSizeId] = useState<string | null>(initialSizeId);

  const selectedSize = useMemo(
    () => sizes.find((s) => s.id === selectedSizeId) || null,
    [selectedSizeId, sizes]
  );

  const canSelectSize = useCallback((sizeId: string) => {
    return sizes.find((s) => s.id === sizeId)?.available ?? false;
  }, [sizes]);

  const selectSize = useCallback((sizeId: string) => {
    if (canSelectSize(sizeId)) {
      setSelectedSizeId(sizeId);
    }
  }, [canSelectSize]);

  return {
    selectedSizeId,
    selectedSize,
    selectSize,
    canSelectSize,
  };
}
EOF

npm run build && npx tsc --noEmit

VERIFY: Hook created, build succeeds
If successful, move to STEP 8

================================================================================
                              STEP 8 TO AGENT
================================================================================

TASK: Extract NavbarSearch Component (45 minutes)

Create file: src/components/layout/_navbar-search.tsx

cat > src/components/layout/_navbar-search.tsx << 'EOF'
import React, { useState } from 'react';
import { Search, X } from 'lucide-react';
import { Input } from '@/components/ui/input';
import { NAVIGATION_STRINGS } from '@/constants/ui-strings';

interface NavbarSearchProps {
  onSearch: (query: string) => void;
  placeholder?: string;
}

export function NavbarSearch({
  onSearch,
  placeholder = NAVIGATION_STRINGS.PRODUCTS
}: NavbarSearchProps) {
  const [query, setQuery] = useState('');

  const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    setQuery(e.target.value);
  };

  const handleClear = () => {
    setQuery('');
  };

  return (
    <div className="relative flex-1 max-w-md">
      <Search className="absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400 w-4 h-4" />
      <Input
        type="text"
        value={query}
        onChange={handleChange}
        placeholder={placeholder}
        className="pl-10 pr-10"
      />
      {query && (
        <button onClick={handleClear} className="absolute right-3 top-1/2">
          <X className="w-4 h-4 text-gray-400" />
        </button>
      )}
    </div>
  );
}
EOF

npm run build && npx tsc --noEmit

VERIFY: Component created, build succeeds
If successful, move to STEP 9

================================================================================
                              STEP 9 TO AGENT
================================================================================

TASK: Extract SizePickerGrid Component (45 minutes)

Create file: src/components/shared/modal/_size-picker-grid.tsx

cat > src/components/shared/modal/_size-picker-grid.tsx << 'EOF'
import React from 'react';
import { Button } from '@/components/ui/button';

interface Size {
  id: string;
  name: string;
  available: boolean;
}

interface SizePickerGridProps {
  sizes: Size[];
  selectedSizeId: string | null;
  onSelectSize: (sizeId: string) => void;
}

export function SizePickerGrid({
  sizes,
  selectedSizeId,
  onSelectSize,
}: SizePickerGridProps) {
  return (
    <div className="grid grid-cols-4 gap-2">
      {sizes.map((size) => (
        <Button
          key={size.id}
          onClick={() => onSelectSize(size.id)}
          disabled={!size.available}
          variant={selectedSizeId === size.id ? 'default' : 'outline'}
          className="w-full"
        >
          {size.name}
        </Button>
      ))}
    </div>
  );
}
EOF

npm run build && npx tsc --noEmit

VERIFY: Component created, build succeeds
If successful, move to STEP 10

================================================================================
                              STEP 10 TO AGENT
================================================================================

TASK: Backup Large Components & Remove Console.log (2 hours)

Create backups:

cp src/components/shared/modal/size-picker-modal.tsx src/components/shared/modal/size-picker-modal.tsx.backup
cp src/components/layout/navbar.tsx src/components/layout/navbar.tsx.backup
cp src/components/ui/sidebar.tsx src/components/ui/sidebar.tsx.backup

Remove console.log statements:

find src -name "*.tsx" -o -name "*.ts" | while read file; do
  sed -i '/console\.log/d' "$file" 2>/dev/null || true
done

Verify removal:

grep -r "console\.log" src --include="*.tsx" --include="*.ts" | wc -l

EXPECTED OUTPUT: 0

Run final verification:

npm run build && npx tsc --noEmit

VERIFY: Console.log removed, build succeeds
If successful, move to STEP 11

================================================================================
                              STEP 11 TO AGENT
================================================================================

TASK: Final Verification & Commit (30 minutes)

Run verification:

echo "=== METRICS ==="
echo "Components: $(find src/components -name "*.tsx" | wc -l)"
echo "Hooks: $(find src/hooks -name "*.ts" | wc -l)"
echo "Services: $(find src/services -name "*.ts" | wc -l)"
echo "Constants: $(find src/constants -name "*.ts" | wc -l)"
echo "Console.log: $(grep -r 'console\.log' src --include="*.tsx" --include="*.ts" | wc -l)"

Commit all changes:

git add -A
git commit -m "refactor: complete frontend cleanup - all 10 steps

- Step 1: Setup & structure
- Step 2: API endpoints constants
- Step 3: UI strings constants
- Step 4: UI timings constants
- Step 5: Routes constants
- Step 6: Component audit
- Step 7: Custom hook (usePickerSize)
- Step 8: Extract NavbarSearch component
- Step 9: Extract SizePickerGrid component
- Step 10: Backup & remove console.log
- Step 11: Final verification"

Push to remote:

git push -u origin refactor/frontend-cleanup-phase-1

VERIFY: All committed and pushed
Status: COMPLETE ✓

================================================================================
                         ALL STEPS COMPLETE
================================================================================

You have successfully:
✓ Created constants for API, UI strings, timings, routes
✓ Audited components for size and quality
✓ Created reusable hooks
✓ Extracted large components
✓ Removed all console.log statements
✓ Committed and pushed all changes

Ready for code review and merge!
