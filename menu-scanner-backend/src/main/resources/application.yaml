# ============================================================
# application.yaml — BASE CONFIG (framework-level only)
# ============================================================
# Dynamic / per-environment config (JWT, telegram, admin, CORS, social,
# cache provider) lives in each profile yaml so it can be changed easily
# per environment (local/dev/h2/prod).
# ============================================================
spring:
  profiles:
    active: "${SPRING_PROFILES_ACTIVE:local}"
  application:
    name: cambodia-emenu-platform

  jpa:
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          batch_size: 50
          fetch_size: 50
          time_zone: Asia/Phnom_Penh
        order_inserts: true
        order_updates: true
        default_batch_fetch_size: 25

  flyway:
    enabled: false

  servlet:
    multipart:
      # Frontend caps each image at 5MB; request size is a touch higher to
      # leave room for multipart encoding overhead (boundaries, headers).
      max-file-size: 5MB
      max-request-size: 6MB

  cache:
    type: none

  data:
    redis:
      host: "${REDIS_HOST:localhost}"
      port: "${REDIS_PORT:6379}"
      password: "${REDIS_PASSWORD:}"
      database: "${REDIS_DATABASE:0}"
      timeout: 2000ms

# ── Actuator & Monitoring ─────────────────────────────────────────────────────
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,caches,loggers
      base-path: /actuator
  endpoint:
    health:
      show-details: when-authorized
      show-components: when-authorized
    prometheus:
      enabled: true
  metrics:
    tags:
      application: ${spring.application.name}
    distribution:
      percentiles-histogram:
        http.server.requests: true
  info:
    env:
      enabled: true

info:
  app:
    name: ${spring.application.name}
    version: "1.0.0"
    description: "Restaurant Management Platform for Cambodia"

# ── Swagger / OpenAPI ─────────────────────────────────────────────────────────
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  api-docs:
    path: /v3/api-docs
    enabled: true

# ── Application (static metadata only) ────────────────────────────────────────
spaces:
  endpoint: "${SPACES_ENDPOINT:https://sgp1.digitaloceanspaces.com}"
  region: "${SPACES_REGION:sgp1}"
  bucket: "${SPACES_BUCKET:emenu-cambodia}"
  access-key: "${SPACES_ACCESS_KEY:DO00QQ6TAV62YPV3DC4U}"
  secret-key: "${SPACES_SECRET_KEY:+ye4MKS7msULnLoNvmEy60KpBGaG8IWGepnTNrdTfTw}"
  cdn-base-url: "${SPACES_CDN_BASE_URL:https://emenu-cambodia.sgp1.cdn.digitaloceanspaces.com}"

app:
  name: "Cambodia E-Menu Platform"
  version: "1.0.0"
  description: "Restaurant Management Platform for Cambodia"
  subscription:
    expiry-soon-days: 7
