# ============================================================
# application.yaml — BASE CONFIG (shared across all profiles)
# ============================================================
# Profile selection:
#   local  → localhost PostgreSQL, debug logging
#   dev    → dev-server PostgreSQL, standard logging
#   prod   → production DB, no Swagger, minimal logging
#
# All sensitive values can be overridden with environment variables.
# ============================================================
spring:
  application:
    name: cambodia-emenu-platform

  # JPA — ddl-auto and show-sql are set per profile
  jpa:
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          batch_size: 50
          fetch_size: 50
          time_zone: Asia/Phnom_Penh
        order_inserts: true
        order_updates: true
        default_batch_fetch_size: 25

  # Flyway — disabled by default; enabled in prod profile only
  flyway:
    enabled: false

  # Cache — beans managed by CacheConfig/RedisConfig directly
  cache:
    type: none

  # Redis
  data:
    redis:
      host: "${REDIS_HOST:localhost}"
      port: "${REDIS_PORT:6379}"
      password: "${REDIS_PASSWORD:}"
      database: "${REDIS_DATABASE:0}"
      timeout: 2000ms

# ── Server ────────────────────────────────────────────────────────────────────
server:
  port: 7070
  max-http-request-header-size: 65536
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html,text/plain
    min-response-size: 2048

# ── JWT ───────────────────────────────────────────────────────────────────────
# JWT_SECRET is REQUIRED in prod (min 64 chars). Dev profiles provide a default.
jwt:
  secret: "${JWT_SECRET}"
  expiration: "${JWT_EXPIRATION:86400000}"
  refresh-token-expiration: "${JWT_REFRESH_EXPIRATION:604800000}"

# ── Actuator & Monitoring ─────────────────────────────────────────────────────
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,caches,loggers
      base-path: /actuator
  endpoint:
    health:
      show-details: when-authorized
      show-components: when-authorized
    prometheus:
      enabled: true
  metrics:
    tags:
      application: ${spring.application.name}
    distribution:
      percentiles-histogram:
        http.server.requests: true
  info:
    env:
      enabled: true

info:
  app:
    name: ${spring.application.name}
    version: "1.0.0"
    description: "Restaurant Management Platform for Cambodia"

# ── Swagger / OpenAPI ─────────────────────────────────────────────────────────
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  api-docs:
    path: /v3/api-docs
    enabled: true

# ── Telegram ──────────────────────────────────────────────────────────────────
telegram:
  bot:
    token: "${TELEGRAM_BOT_TOKEN:}"
    enabled: true
    group-chat-id: "${TELEGRAM_GROUP_CHAT_ID:}"
    webhook-url: "${TELEGRAM_WEBHOOK_URL:}"
    auto-setup: true

# ── Application ───────────────────────────────────────────────────────────────
app:
  name: "Cambodia E-Menu Platform"
  version: "1.0.0"
  description: "Restaurant Management Platform for Cambodia"
  init:
    create-admin: true
    admin-email: "${APP_ADMIN_EMAIL:}"
    admin-password: "${APP_ADMIN_PASSWORD:}"
  subscription:
    expiry-soon-days: 7
  cache:
    provider: "${CACHE_PROVIDER:caffeine}"
  security:
    cors:
      allowed-origins: "${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}"
    token-cleanup:
      enabled: true
  social:
    telegram:
      bot-token: "${TELEGRAM_BOT_TOKEN:}"
      enabled: true
    google:
      client-id: "${GOOGLE_CLIENT_ID:}"
      client-secret: "${GOOGLE_CLIENT_SECRET:}"
      enabled: true
    facebook:
      app-id: "${FACEBOOK_APP_ID:}"
      app-secret: "${FACEBOOK_APP_SECRET:}"
      enabled: false
